# 2026-04-14

- 11:53 保存 Claude Code 实战最佳实践与参考材料：
  - 主题：`shanraisshan/claude-code-best-practice` 仓库梳理 + `forrestchang/andrej-karpathy-skills` 的 `CLAUDE.md` 规范。
  - 核心点：
    - 推荐采用 **Command → Agent → Skill** 分层编排，避免单一大提示词。
    - `CLAUDE.md` 作为项目行为与流程约束入口，控制体量、按目录分层（monorepo 场景祖先加载 + 子目录懒加载）。
    - Subagent 要小而专：限制工具权限、先 plan 后 execute，必要时 worktree 隔离。
    - Skill/Command 用 `paths` 限定自动触发范围，降低上下文噪音。
    - MCP 采用少而精策略（文档检索 + 浏览器调试优先），避免盲目堆叠。
    - 任务执行强调目标可验证（测试/复现/通过标准），并控制上下文膨胀。
    - `forrestchang` 的 `CLAUDE.md` 四条通用守则：先思考、简单优先、手术式修改、目标驱动验证。
  - 可复用洞察：
    - 后续落地时可先做一个最小模板：`CLAUDE.md` + 1 command + 1 agent + 1 skill。
    - 可将上述规范沉淀为团队知识库条目：架构分层、权限策略、上下文治理、验证闭环。
